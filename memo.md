# 📝 PDF 업로드 시 환급금 미계산 및 재분석 강제 현상 분석 및 해결 메모

## 1. 문제 현상 원인 분석
* **원인**: PDF 파일 분석 시 일부 값(총급여, 산출세액, 기존 결정세액 등)이 PDF 양식의 차이나 파싱 실패로 인해 `0`으로 읽혀 들어오는 경우가 있습니다. 이로 인해 최초 PDF 업로드 시 환급액이 `0`원으로 노출되었습니다.
* **추가 문제 (핵심)**: 환급액이 `0`원으로 채워진 후, 사용자가 테이블의 셀(총급여, 산출세액, 결정세액 등)을 수동으로 직접 올바르게 수정 기입하더라도 **셀 편집 핸들러(`updateYearField`) 내에 실시간 재계산 기능(`recalculateYearData`)이 누락**되어 있어 환급금이 여전히 `0`원으로 멈춰 있었습니다.
* **재분석 시 계산되었던 이유**: 수동으로 셀 값을 고친 후 `🔄 PDF 재분석`을 클릭하면, 파서가 다시 작동하면서 PDF의 빈 값 대신 **사용자가 테이블 셀에 수동으로 입력해둔 값들을 그대로 읽어와(`updatedYears[idx]?.taxBase` 등) 전체 수식 재계산(`recalculateYearData`)을 강제 수행**했기 때문에 그제야 정상 환급액이 계산되어 노출되었던 것입니다.

---

## 2. 해결 및 조치 완료 사항
사용자가 테이블 내의 텍스트 필드를 직접 편집할 때마다 실시간으로 수식이 작동하도록 구조를 전면 개선했습니다.

1. **실시간 셀 편집 계산 기능 도입 (`WageSettlementTable.tsx`)**
   * 테이블의 세무 정보 셀(`salaryTotal`, `taxBase`, `decisionTax`, `localTax`, `childReductionApply`, `childReductionApplyAmt` 등)을 편집하여 상태를 변경하는 `updateYearField` 함수를 전면 보완했습니다.
   * 이제 셀 값을 타이핑하여 변경하는 즉시 **`recalculateYearData` 엔진이 실시간으로 트리거**되어 주민등록번호와 취업일자를 기준으로 감면율 및 최종 예상 환급금(`courtFee`)을 즉각 계산하여 화면에 실시간으로 갱신해 줍니다.
   
2. **주민등록번호 및 취업일자 매핑 정합성 검증**
   * 사용자가 화면에 주민등록번호(RRN)와 취업일자(Hire Date)를 먼저 입력해둔 상태에서 PDF를 업로드할 때, 기존에 입력된 full-text 주민등록번호가 PDF 내의 마스킹된 주민등록번호(`960126-1******`)로 인해 **덮어씌워지지 않고 기존 값을 안전하게 유지하도록 처리**되어 있음을 확인했습니다.
   * `checkYouthEligibility` 엔진도 마스킹된 RRN과 일반 RRN 모두에 대하여 정상적으로 연령 적격성 판정을 수행하도록 완벽하게 교차 검증되었습니다.

---

## 3. 요약
이제 **주민등록번호와 취업일자를 먼저 입력한 뒤 PDF를 업로드**하거나, 업로드된 상태에서 **테이블 값을 수동으로 직접 수정**하더라도 별도의 `🔄 PDF 재분석` 버튼을 누를 필요 없이 **그 즉시 환급금이 실시간으로 자동 연산되어 화면에 반영**됩니다.
